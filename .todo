Publish to npm

Repos Action:
-Create (Should also ask to git clone in current directory)(Apply rammasage-tek automaticaly)
-Delete (List all repos and ask before deleting)

Acl Actions
- Add (Get a list of all repos, can click on them then add an user)

change inital indexes on menu to go back by default